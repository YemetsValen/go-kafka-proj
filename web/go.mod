// This stub module exists solely to keep the parent `go ./...` from
// recursing into stray .go files in node_modules. The web/ directory is a
// JavaScript / TypeScript workspace; it does not contain Go source.
module github.com/YemetsValen/go-kafka-proj/web

go 1.25
